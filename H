-- [[ DRAGON BLOX MOBILE HUB - FIX FOR DELTA X VNG ]] --
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local TweenService = game:GetService("TweenService")
local VirtualUser = game:GetService("VirtualUser")
local TeleportService = game:GetService("TeleportService")
local UserInputService = game:GetService("UserInputService")

-- Kiểm tra thư mục chứa UI an toàn cho Mobile
local GuiParent = nil
local success, err = pcall(function()
	GuiParent = game:GetService("CoreGui")
end)
if not success or not GuiParent then
	GuiParent = LocalPlayer:WaitForChild("PlayerGui")
end

-- 1. TRẠNG THÁI CHỨC NĂNG
local States = {
	AutoFarm = false, AutoRaid = false, AutoDungeon = false, NoCooldown = false,
	Lifesteal = false, AutoBeast = false, AutoTransform = false, AutoAFK = false,
	AutoAtomMax = false, AutoDLS = false
}

local Config = {
	AttackRange = 25, DamageAmount = 50, AttackCooldown = 0.6,
	DungeonPlaceId = 3254817462, LifestealPercent = 0.25,
	BeastRequiredKi = 500000, BossName = "Atom Max", NextComboIndex = 1,
	MenuVisible = true, CurrentFormIndex = 0, BaseDamage = 50
}

local DLS_COMBO = {
	{Name = "DLS 1", Damage = 150, Cooldown = 0.5},
	{Name = "DLS 2", Damage = 300, Cooldown = 0.5},
	{Name = "DLS 3", Damage = 600, Cooldown = 0.8}
}

local TRANSFORMS = {
	{Name = "SSJ 1", RequiredKi = 1000, DamageMultiplier = 1.5, AuraColor = Color3.fromRGB(255, 255, 0)},
	{Name = "SSJ 2", RequiredKi = 5000, DamageMultiplier = 2.5, AuraColor = Color3.fromRGB(255, 220, 0)},
	{Name = "SSJ 3", RequiredKi = 25000, DamageMultiplier = 4.0, AuraColor = Color3.fromRGB(255, 140, 0)},
	{Name = "SSJ God", RequiredKi = 100000, DamageMultiplier = 6.5, AuraColor = Color3.fromRGB(255, 0, 50)},
	{Name = "SSJ Blue", RequiredKi = 500000, DamageMultiplier = 10.0, AuraColor = Color3.fromRGB(0, 180, 255)},
}

-- Xóa UI cũ chống đè màn hình
if GuiParent:FindFirstChild("DragonBloxTestingHub") then
	GuiParent:FindFirstChild("DragonBloxTestingHub"):Destroy()
end

-- 2. KHỞI TẠO GIAO DIỆN MOBILE
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "DragonBloxTestingHub"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = GuiParent

local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 240, 0, 435)
MainFrame.Position = UDim2.new(0.1, 0, 0.15, 0)
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 17)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Parent = ScreenGui

local FrameCorner = Instance.new("UICorner")
FrameCorner.CornerRadius = UDim.new(0, 10)
FrameCorner.Parent = MainFrame

-- HỆ THỐNG KÉO THẢ DI ĐỘNG (TOUCH COMPATIBLE)
local dragToggle, dragStart, startPos
MainFrame.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
		dragToggle = true
		dragStart = input.Position
		startPos = MainFrame.Position
		input.Changed:Connect(function()
			if input.UserInputState == Enum.UserInputState.End then dragToggle = false end
		end)
	end
end)
MainFrame.InputChanged:Connect(function(input)
	if dragToggle and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
		local delta = input.Position - dragStart
		MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
	end
end)

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0, 40)
Title.BackgroundColor3 = Color3.fromRGB(24, 24, 28)
Title.Text = "⚡ DRAGON BLOX HUB (DELTA X) ⚡"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 10
Title.Parent = MainFrame

local TitleCorner = Instance.new("UICorner")
TitleCorner.CornerRadius = UDim.new(0, 10)
TitleCorner.Parent = Title

-- NÚT BẤM TRÒN ẨN/HIỆN MINI (Kéo thả được)
local ToggleButton = Instance.new("TextButton")
ToggleButton.Size = UDim2.new(0, 42, 0, 42)
ToggleButton.Position = UDim2.new(0.02, 0, 0.15, 0)
ToggleButton.BackgroundColor3 = Color3.fromRGB(20, 20, 22)
ToggleButton.Text = "👁"
ToggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleButton.Font = Enum.Font.GothamBold
ToggleButton.TextSize = 16
ToggleButton.Active = true
ToggleButton.Parent = ScreenGui

Instance.new("UICorner", ToggleButton).CornerRadius = UDim.new(1, 0)
local ButtonStroke = Instance.new("UIStroke", ToggleButton)
ButtonStroke.Color = Color3.fromRGB(0, 255, 120)
ButtonStroke.Thickness = 1.5

local tDragToggle, tDragStart, tStartPos
ToggleButton.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
		tDragToggle = true
		tDragStart = input.Position
		tStartPos = ToggleButton.Position
		input.Changed:Connect(function()
			if input.UserInputState == Enum.UserInputState.End then tDragToggle = false end
		end)
	end
end)
ToggleButton.InputChanged:Connect(function(input)
	if tDragToggle and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
		local delta = input.Position - tDragStart
		ToggleButton.Position = UDim2.new(tStartPos.X.Scale, tStartPos.X.Offset + delta.X, tStartPos.Y.Scale, tStartPos.Y.Offset + delta.Y)
	end
end)

ToggleButton.MouseButton1Click:Connect(function()
	Config.MenuVisible = not Config.MenuVisible
	local targetSize = Config.MenuVisible and UDim2.new(0, 240, 0, 435) or UDim2.new(0, 240, 0, 0)
	TweenService:Create(MainFrame, TweenInfo.new(0.2, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), {Size = targetSize}):Play()
	ToggleButton.Text = Config.MenuVisible and "👁" or "❌"
	ButtonStroke.Color = Config.MenuVisible and Color3.fromRGB(0, 255, 120) or Color3.fromRGB(255, 50, 50)
end)

local function createToggleButton(name, text, positionY)
	local button = Instance.new("TextButton")
	button.Size = UDim2.new(0, 210, 0, 32)
	button.Position = UDim2.new(0, 15, 0, positionY)
	button.BackgroundColor3 = Color3.fromRGB(28, 28, 32)
	button.Text = "❌ " .. text
	button.TextColor3 = Color3.fromRGB(170, 170, 170)
	button.Font = Enum.Font.GothamSemibold
	button.TextSize = 10
	button.ClipsDescendants = true
	button.Parent = MainFrame
	
	Instance.new("UICorner", button).CornerRadius = UDim.new(0, 6)
	local UIStroke = Instance.new("UIStroke", button)
	UIStroke.Color = Color3.fromRGB(45, 45, 50)

	button.MouseButton1Click:Connect(function()
		States[name] = not States[name]
		if States[name] then
			button.BackgroundColor3 = Color3.fromRGB(22, 44, 28)
			UIStroke.Color = Color3.fromRGB(0, 255, 120)
			button.TextColor3 = Color3.fromRGB(0, 255, 120)
			button.Text = "✔ " .. text
		else
			button.BackgroundColor3 = Color3.fromRGB(28, 28, 32)
			UIStroke.Color = Color3.fromRGB(45, 45, 50)
			button.TextColor3 = Color3.fromRGB(170, 170, 170)
			button.Text = "❌ " .. text
		end
	end)
end

createToggleButton("AutoFarm", "Auto Farm Quái Thường", 50)
createToggleButton("AutoRaid", "Auto Chạy Ải Vượt Raid", 87)
createToggleButton("AutoDungeon", "Auto Vào Phòng Dungeon", 124)
createToggleButton("NoCooldown", "Bỏ Hồi Chiêu (No Cooldown)", 161)
createToggleButton("Lifesteal", "Auto Hút Máu Hồi HP", 198)
createToggleButton("AutoBeast", "Auto Vận Sức Hóa Beast", 235)
createToggleButton("AutoTransform", "Auto Tiến Hóa Cấp SSJ", 272)
createToggleButton("AutoAFK", "Chống Treo Máy (Anti-AFK)", 309)
createToggleButton("AutoAtomMax", "Auto Săn Siêu Boss Atom", 346)
createToggleButton("AutoDLS", "Auto Combo Chuỗi Skill DLS", 383)

-- 3. LOGIC QUÉT MỤC TIÊU
local function getTarget(range, specificName)
	local char = LocalPlayer.Character
	local root = char and char:FindFirstChild("HumanoidRootPart")
	if not root then return nil, nil end
	
	local nearest, shortDist = nil, range
	local enemyFolder = workspace:FindFirstChild("Enemies") or workspace:FindFirstChild("NPCs") or workspace
	
	for _, obj in pairs(enemyFolder:GetChildren()) do
		if obj:IsA("Model") and obj ~= char then
			if not specificName or (specificName and string.find(string.lower(obj.Name), string.lower(specificName))) then
				local tHumanoid = obj:FindFirstChildOfClass("Humanoid")
				local tRoot = obj:FindFirstChild("HumanoidRootPart")
				if tHumanoid and tRoot and tHumanoid.Health > 0 then
					local dist = (root.Position - tRoot.Position).Magnitude
					if dist < shortDist then
						shortDist = dist
						nearest = obj
					end
				end
			end
		end
	end
	if nearest then
		return nearest:FindFirstChildOfClass("Humanoid"), nearest:FindFirstChild("HumanoidRootPart")
	end
	return nil, nil
end

-- 4. VÒNG LẶP LIÊN TỤC CHẠY NGẦM AN TOÀN CHO MOBILE
task.spawn(function()
	while true do
		task.wait(Config.AttackCooldown)
		local char = LocalPlayer.Character
		local humanoid = char and char:FindFirstChildOfClass("Humanoid")
		local rootPart = char and char:FindFirstChild("HumanoidRootPart")
		
		if char and humanoid and humanoid.Health > 0 and rootPart then
			if States.AutoAFK then
				pcall(function()
					VirtualUser:CaptureController()
					VirtualUser:ClickButton2(Vector2.new(0, 0))
				end)
			end

			local targetHum, targetRoot = nil, nil
			if States.AutoAtomMax then
				targetHum, targetRoot = getTarget(math.huge, Config.BossName)
				if targetRoot then
					rootPart.CFrame = targetRoot.CFrame * CFrame.new(0, 9, 0)
					rootPart.Velocity = Vector3.new(0, 0, 0)
				end
			elseif States.AutoRaid then
				targetHum, targetRoot = getTarget(math.huge)
				if targetRoot and (rootPart.Position - targetRoot.Position).Magnitude > Config.AttackRange then
					humanoid:MoveTo(targetRoot.Position)
				end
			elseif States.AutoFarm then
				targetHum, targetRoot = getTarget(Config.AttackRange)
			end
			
			if targetHum and targetHum.Health > 0 then
				pcall(function() targetHum:TakeDamage(Config.DamageAmount) end)
				if States.Lifesteal then
					humanoid.Health = math.min(humanoid.MaxHealth, humanoid.Health + (Config.DamageAmount * Config.LifestealPercent))
				end
			end
			
			if States.AutoDLS then
